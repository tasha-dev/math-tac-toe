// Codes by mahdi tasha
// Importing config types
/** @types {import('next').NextConfig} */

// Defining the config of nextJS
const nextConfig = {};

// Exporting the config as default
export default nextConfig;
